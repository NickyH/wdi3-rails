%h1= @user.name
%br
%div= image_tag(@user.image)
%br
%td= link_to('Edit', edit_user_path(@user), :class => 'button radius tiny')
%td= link_to('Delete', @user, :method => 'delete', :confirm => 'Are you sure?', :class => 'button alert radius tiny')