- if @auth.present?
  = link_to('New Product', new_product_path, :remote => true) if @auth.is_admin?

#search_top
  #search
    = text_field_tag :search, nil, :autofocus => true

#form

#products
  = render :partial => 'products/index'


#map_canvas
  = render :partial => 'products/map'