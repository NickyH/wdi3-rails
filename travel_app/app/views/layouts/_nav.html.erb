 <!-- Header and Nav -->
  <nav class="top-bar">
    <ul class="title-area">
      <!-- Title Area -->
      <li class="name">
        <h1>
          <a href="#">
            Destination Unknown
          </a>
        </h1>
      </li>
      <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
    </ul>


    <section class="top-bar-section">
      <!-- Left Nav Section -->
      <ul class="left">
        <li class="divider"></li>
        <li class="has-dropdown">
          <% if @auth.present? %>
          <li><%= link_to('Logout', login_path, :method => :delete) %></li>
        <% else %>
          <li><%= link_to('Login', login_path) %></li>
          <li class="divider"></li>
        <li><%= link_to('Sign Up!', new_user_path) %></li>
        <% end %>
      </ul>

      <!-- Right Nav Section -->
      <ul class="right">
        <li class="divider"></li>
        <li class="has-dropdown">
           <% if @auth.present? %>
           <li class="divider"></li>
           <li><%= link_to(@auth.name, user_path(@auth)) %></li>
           <li><%= link_to('MyTrips', itineraries_path) %></li>
         <li class="divider"></li>
        <li><%= link_to('Profile', user_path(@auth)) %></li>
        <li class="divider"></li>
        <li><%= link_to('Find Trips', new_user_path) %></li>

         <% end %>

        </ul>

    </section>
  </nav>


  <!-- End Header and Nav -->
