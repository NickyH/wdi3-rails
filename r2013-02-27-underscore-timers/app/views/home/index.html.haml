%h1 underscore and timers
%p
  = text_field_tag 'number', nil, :autofocus => true
  = button_tag 'Add Number', :id => 'addnumber'
  = button_tag 'Square', :id => 'square'
  = button_tag 'Function', :id => 'fn'
  = button_tag 'Start', :id => 'start'
  = button_tag 'Stop', :id => 'stop'
  #shownumbers
    .clear